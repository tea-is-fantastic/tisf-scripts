type: edit
path:
  - $rn_screens
  - {{args.[0]}}Screen.tsx
code: |
  import React from 'react';
  import { Text, View } from 'react-native';

  export const {{args.[0]}}Screen: React.FC = () => (
    <View
      style=\{{
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
      }}
    >
      <Text>{{args.[0]}} Screen</Text>
    </View>
  );
